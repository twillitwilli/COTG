using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PuzzleLists : MonoBehaviour
{
    public List<GameObject> anyNormalRoom, deadendRoom, dungeonRoom, ERoom, ECutRoom, EWRoom, EWCutRoom, SRoom, SCutRoom, SERoom, SECutRoom, SWRoom, SWCutRoom,
        WRoom, WCutRoom, LongLeftRoom, LongRightRoom, LongRoom, ZigZagRoom;
}
